const colors = require('./colors')
const layout = require('./layout')
const typography = require('./typography')
const ui = require('./ui')

module.exports = {
  ...colors,
  ...layout,
  ...typography,
  ...ui
}
